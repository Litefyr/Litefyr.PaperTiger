prototype(Sitegeist.PaperTiger:Field.Dropdown) {
    emptyLabel.@process.required = ${this.isRequired && !this.label ? value + ' *' : value}
    renderer.placeholder = ${props.emptyOptionEnabled ? props.emptyLabel : null}
}
