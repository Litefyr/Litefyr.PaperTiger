prototype(Sitegeist.PaperTiger:Field.Number) {
    placeholder.@process.required = ${this.isRequired && !this.label ? value + ' *' : value}
    renderer.placeholder = ${props.placeholder}

    prototype(Neos.Fusion.Form:Input) {
        attributes.inputmode = 'numeric'
    }
}
