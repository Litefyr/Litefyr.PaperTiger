prototype(Litefyr.Presentation:Module.Address) {
    form = Sitegeist.PaperTiger:Form {
        @if.set = ${q(node).property('additionalPaperTiger')}
        contentElement = false
        inMultiColumn = true
        animationMedia >
        animationText >
    }
}
